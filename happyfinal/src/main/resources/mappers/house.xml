<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.happyhouse.model.mapper.HouseMapper">

	<select id="getHouseList" parameterType="map" resultType="houseDto">
		select * from houseinfo
		where dong = #{mydong}
	</select>
	
	<select id="getAptList" parameterType="String" resultType="houseDto">
		select no,AptName, dealAmount as price, dong, area
		 from housedeal
		where AptName LIKE #{aptName}
	</select>
	
	<select id="chartList"  resultType="houseDto">
		select dong, avg(cast(dealAmount as unsigned )) as price
		from  housedeal
		group by dong
		order by avg(dealAmount) desc limit 5
	</select>
	
</mapper>